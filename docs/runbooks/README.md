# Runbooks

One-off procedures — run per machine or per install, not per task.

| Read when | Runbook |
|---|---|
| Wiring the redirect hook and agent rules into a Claude install | [hooks-setup.md](hooks-setup.md) |

`npm run claude:install` (or `npx @nickw8/bash-mcp --install-claude`) does the copy step;
`hooks-setup.md` covers what it writes and how to wire the hook by hand.
